import React from 'react';
import Todo from "../todo";

const TodoList = () => {
    return (
        <ul className='list-group mb-3'>
            <Todo/>
        </ul>
    );
};

export default TodoList;